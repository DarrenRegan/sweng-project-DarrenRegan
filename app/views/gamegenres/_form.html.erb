<%= form_for(@gamegenre) do |f| %>
  <% if @gamegenre.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gamegenre.errors.count, "error") %> prohibited this gamegenre from being saved:</h2>

      <ul>
      <% @gamegenre.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :difficulty %><br>
    <%= f.number_field :difficulty %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
